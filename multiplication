#define _CRT_SECURE_NO_WARNINGS
#include <iostream>

int main(void)
{
	int x,y;
	scanf("%d", &x);
	scanf("%d", &y);
	
	
	printf("  %d\n", (y % 10)*x);
	printf(" %d\n", ((y % 100) / 10) * x);
	printf("%d\n", (y / 100) * x);
	printf("%d",x*y);
	return 0;
}
